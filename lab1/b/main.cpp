#include <iostream>
using namespace std;
int main() {
    long long a, b;
    freopen("aplusbb.in", "r", stdin);
    freopen("aplusbb.out", "w", stdout);
    cin >> a >> b;
    cout << a + b * b;
    return 0;
}
